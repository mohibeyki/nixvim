{
  plugins.trouble = {
    enable = true;
    settings = {
      auto_preview = true;
      use_diagnostic_signs = true;
      modes = {
        diagnostics = {
          auto_close = true;
        };
      };
    };
  };
}
